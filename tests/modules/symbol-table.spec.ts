import {
  SymbolTable,
  SymbolTableBuilder,
} from '../../src/modules/symbol-table';

describe(SymbolTable.name, () => {
  it('empty', () => {
    expect(1).toBe(1);
  });
});

describe(SymbolTableBuilder.name, () => {
  it('empty', () => {
    expect(1).toBe(1);
  });
});
